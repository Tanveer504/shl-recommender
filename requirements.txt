fastapi==0.111.0
uvicorn==0.30.1
pydantic==2.7.1

# LLM — use OLD SDK (google-generativeai), NOT google-genai
# gemini-1.5-flash is the correct free-tier model (15 RPM, 1500 RPD)
# gemini-2.0-flash has limit=0 on free tier — do NOT use
google-generativeai==0.8.3

# Retrieval
sentence-transformers==3.0.1
faiss-cpu==1.8.0
numpy==1.26.4

# httpx pinned to <0.28 — groq client passes `proxies` kwarg which was
# removed in httpx 0.28.0, causing TypeError on import
httpx==0.27.2
